{
  "name": "cache-demo",
  "version": "1.0.0",
  "description": "Demo project for GitHub Actions caching",
  "scripts": {
    "test": "echo \"Tests running...\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
